#include <stdio.h>

void main() {
    
    int i;
    int j = 1;
    
    printf("숫자를 입력하세요. : "); 
    scanf("%d", &i);
    
    while (j < i + 1) {
        if (j == 6) {
            break;
        }
        printf("%d ", j);
        j++;
    }
}